<-- 0. INITIAL PROMPT -->

You are a full product builder LLM.
Build a runnable, clickable UI prototype (frontend-only) using:

- Next.js (latest stable) with App Router
- TypeScript
- Tailwind CSS
- shadcn/ui components
- lucide-react icons
- localStorage persistence
- No external backend required

GOAL:
Create Stage 1 prototype for “One Piece TCG Seller/Collector CRM + Vendor Page”.
Implement frontend flows end-to-end with realistic mock data and a mock API layer. 
All network calls must be stubs with latency and occasional failures.

STRICT RULES:
- Frontend only. No real external API integrations.
- All data comes from local mock DB seeded in code and persisted to localStorage.
- Public vendor page must be accessible via route /vendor/[handle]
- Pricing is centralized per Card Profile (global) and reflected in all inventory items that reference that profile.
- Output must be runnable locally with clear README.

------------------------------------------------------------
PROJECT SETUP (must do)
------------------------------------------------------------
1) Create a Next.js app (App Router) with TypeScript.
2) Configure Tailwind.
3) Install and set up shadcn/ui.
4) Add a simple toast system (shadcn/toast).
5) Implement a layout with:
   - Desktop: sidebar nav
   - Mobile: bottom nav
6) Add routing for pages below.

Recommended folder structure:

/app
  / (landing)
  /onboarding
  /(app)
    /dashboard
    /inventory
    /inventory/add
    /pricing
    /settings
  /vendor/[handle]
/components
  /layout (Sidebar, BottomNav, Topbar)
  /inventory (KanbanBoard, ItemCard, ItemDrawer, StageColumn)
  /cards (CardSearch, CardProfileBadge)
  /pricing (PricingTable, RefreshButton)
  /common (EmptyState, Skeletons, ErrorBanner, CopyLinkButton)
/lib
  mockApi.ts
  storage.ts
  seed.ts
  types.ts
  utils.ts

------------------------------------------------------------
DATA MODEL (types.ts)
------------------------------------------------------------
Define types:

CardProfile:
- id (string)
- name (string)
- set (string)
- rarity (string)
- cardNumber (string)
- imageUrl (string)  // can be placeholder

PricingSnapshot:
- cardProfileId
- rawPrice (number)
- sealedPrice (number | null)
- updatedAt (ISO string)
- source (string: "Mock:eBay" | "Mock:TCGPlayer")

SellerProfile:
- handle (string, used in vendor URL)
- shopName
- locationCountry
- locationCity
- paymentsAccepted (string[])
- meetupsEnabled (boolean)
- shippingEnabled (boolean)
- socials: { instagram?, tiktok?, discord?, tcgplayer?, ebay? }
- wishlistText (string)

InventoryStage (enum strings):
- "ACQUIRED"
- "IN_TRANSIT"
- "IN_STOCK_UNGRADED"
- "BEING_GRADED"
- "UNGRATED_FOR_SALE"
- "GRADED_FOR_SALE"

InventoryItem:
- id
- cardProfileId
- itemType ("RAW" | "GRADED" | "SEALED")
- gradeProvider? ("PSA" | "BGS" | null)
- gradeValue? (number | null)
- quantity (number)
- stage (InventoryStage)
- listingPrice (number | null)
- acquisitionPrice (number | null)
- photos: { front?: string; back?: string; extra?: string[] } // store object URLs
- createdAt

------------------------------------------------------------
MOCK DATA (seed.ts)
------------------------------------------------------------
Seed at least:
- 30 CardProfiles (One Piece) with plausible names/sets/numbers + placeholder imageUrl
- 30 PricingSnapshots for those CardProfiles
- 1 SellerProfile seeded
- Inventory seeded:
  - 10 RAW items
  - 5 GRADED items
  - 3 SEALED products (quantity > 1 for at least one)
  - distributed across stages

NOTE:
You do not need real OPTCG API data; use realistic placeholders.

------------------------------------------------------------
LOCAL STORAGE (storage.ts)
------------------------------------------------------------
Implement storage with versioning:
- KEY: "opcrm_v1"
- functions:
  - loadState()
  - saveState(state)
  - resetToSeed()

Persist:
- sellerProfile
- cardProfiles (seeded, mostly static)
- pricingSnapshots
- inventoryItems

------------------------------------------------------------
MOCK API (mockApi.ts) — REQUIRED
------------------------------------------------------------
Implement async functions with:
- latency 200–900ms
- 10% random failure
- consistent errors (return { ok:false, error } or throw)

Functions:
- getCurrentUser(): SellerProfile
- updateProfile(patch: Partial<SellerProfile>)
- listCardProfiles(query?: string): CardProfile[]
- getCardProfile(id)
- listInventory(): InventoryItem[]
- createInventoryItem(payload)
- updateInventoryItem(id, patch)
- deleteInventoryItem(id)
- listPricing(): PricingSnapshot[]
- refreshPricing(): PricingSnapshot[]
    - updates pricingSnapshots for all card profiles randomly within sane bounds
    - updates updatedAt
    - returns snapshots

IMPORTANT: pricingSnapshots are global.
InventoryItems reference CardProfile IDs; market price is derived by joining pricingSnapshots + CardProfile.

------------------------------------------------------------
PAGES & REQUIRED UX
------------------------------------------------------------

1) Landing (/)
- Hero section explaining product
- CTA: “Get started”
- If existing profile exists -> “Go to dashboard”

2) Onboarding (/onboarding)
- Form for SellerProfile fields:
  shopName, handle, location, payments (multi select),
  meetups toggle, shipping toggle,
  socials inputs (no verification, just UI)
- Submit -> saves profile -> routes to /dashboard

3) App Layout (/(app)/*)
- Sidebar nav items: Dashboard, Inventory, Pricing, Settings
- Mobile bottom nav for same
- Topbar shows shopName + “View Public Page” button linking to /vendor/[handle]

4) Dashboard (/(app)/dashboard)
- KPI cards: total items, for-sale items, total market value (approx), stages breakdown
- “Quick add” button -> /inventory/add
- “Recent price updates” banner (reads last updated timestamp)
- Show small list of items recently modified (mock)

5) Inventory Kanban (/(app)/inventory)
- Kanban columns per stage
- Move items between stages:
  - Prefer drag-and-drop (can use a light library) OR stage dropdown if easier
- Item card shows:
  image, name, set, condition/grade, listing price, market ref price
- Clicking item opens Item Drawer/Modal with:
  - images upload front/back/extra
  - stage selector
  - for sale toggle (mapped to stage switch into appropriate for-sale stage)
  - listing price input
  - acquisition price input
  - show “Market Price” and “Last updated”
  - delete button with confirmation
- Provide:
  - skeleton loaders
  - empty states
  - toast on save/delete
  - error UI + retry when mock API fails

6) Add Item (/(app)/inventory/add)
- Stepper UI (shadcn):
  Step 1: Search/select CardProfile (search box + results list)
  Step 2: Item details (RAW/GRADED/SEALED, grade provider/value, quantity, acquisitionPrice, initial stage)
  Step 3: Photos upload
  Save -> routes back to inventory and highlights newly added item.

CRITICAL RULE:
User must select from existing CardProfiles (no free-text card creation).

7) Pricing (/(app)/pricing)
- Table of CardProfiles with:
  card image, name, set, rawPrice, sealedPrice, updatedAt, source
- “Refresh pricing” button -> calls refreshPricing()
- After refresh, dashboard/kanban must reflect updated prices automatically
- Show progress/loading, toast, and errors

8) Settings (/(app)/settings)
- Edit SellerProfile
- Wishlist (textarea)
- Button “Reset demo data” -> resetToSeed()

9) Public Vendor Page (/vendor/[handle])
- Read-only page
- Shows seller profile card:
  shopName, location, payments accepted, shipping/meetups
- Shows items for sale (derived from inventory stages “UNGRADED_FOR_SALE” and “GRADED_FOR_SALE”):
  - grid with filters: search by name, graded/ungraded toggle, price range (simple)
- Each item card shows:
  image, listing price, market ref price, condition/grade
- Wishlist section (“Looking to buy”)
- Copy link button

------------------------------------------------------------
UI COMPONENT REQUIREMENTS (use shadcn/ui)
------------------------------------------------------------
Must use:
- Card, Button, Input, Select, Checkbox, Switch, Tabs or Stepper, Dialog/Sheet, Table, Badge, Toast
- lucide icons
- consistent spacing + responsive design

------------------------------------------------------------
QUALITY REQUIREMENTS
------------------------------------------------------------
- Type-safe throughout (TypeScript)
- Clean code structure
- Reusable components
- No broken routes
- No console errors
- No external network dependency

------------------------------------------------------------
README (required)
------------------------------------------------------------
Include:
- Setup commands
- How mock API works
- Where to adjust seed data
- How future real API would replace mockApi.ts (mapping table of functions → future endpoints)

------------------------------------------------------------
OUTPUT
------------------------------------------------------------
Return the full codebase (all necessary files) and README.
Ensure it runs with `npm install` and `npm run dev`.


<-- 1. FEATURE PROMPT -->

You are continuing work on an existing Next.js (App Router) + TypeScript + Tailwind + shadcn/ui prototype.
Current state:
- Inventory Kanban exists but DOES NOT support drag-and-drop.
- Inventory currently has only Kanban view.
Your task:
1) Implement drag-and-drop moving of inventory items between stages in the Kanban.
2) Add a view mode toggle: Kanban view (default) and List view.
3) Preserve existing mock API layer and localStorage persistence.
4) Ensure all changes are production-quality and fully clickable.

------------------------------------------------------------
A) DRAG-AND-DROP REQUIREMENTS (MANDATORY)
------------------------------------------------------------
Implement real drag-and-drop with a lightweight library.

Preferred library order:
1) @dnd-kit/core + @dnd-kit/sortable (recommended)
2) react-beautiful-dnd (only if dnd-kit is problematic)
3) react-dnd (fallback)

Hard requirements:
- User can drag an InventoryItem card from one stage column to another.
- On drop:
  - Update the item.stage immediately (optimistic UI)
  - Call mockApi.updateInventoryItem(id, { stage: newStage })
  - If mockApi fails (random 10%):
      - Show toast error
      - Revert the item to its previous stage (rollback)
      - Keep UI consistent (no duplicates)
- Maintain smooth UX:
  - Show drag overlay preview of the dragged card
  - Show drop target highlight on hovered column
  - Preserve scroll usability on mobile (reasonable touch support)

Implementation guidance:
- Model the Kanban board as:
  - droppable containers per stage column
  - draggable cards per item
- Use stable IDs:
  - draggable id: inventory item id
  - droppable id: stage enum string
- Ensure that dragging within same column preserves order in that column OR keep a stable sort key (optional).
  If ordering is not implemented, keep a deterministic order (e.g., createdAt desc).

Files to create/update:
- /components/inventory/KanbanBoard.tsx
- /components/inventory/StageColumn.tsx
- /components/inventory/ItemCard.tsx
- /components/inventory/dnd.ts (helpers: stages array, sensors, collision strategy)
- /lib/types.ts (if stage enums need alignment)

------------------------------------------------------------
B) INVENTORY VIEW MODE TOGGLE (KANBAN <-> LIST)
------------------------------------------------------------
Add a UI control to switch inventory presentation.

Requirements:
- Add a segmented control / toggle in Inventory page header:
  - “Kanban” and “List”
  Use shadcn/ui Tabs or ToggleGroup or Segmented control style.
- Default view: Kanban
- Persist user choice in localStorage (e.g., key: "inventory_view_mode")
- Switching view must be instant and not refetch data.
- Both views must support opening the Item detail drawer/modal on click.
- Both views must show the same core info:
  - image thumbnail, card name, set, condition/grade, stage, listing price, market ref price

List view requirements:
- Display items as a table (shadcn/ui Table):
  Columns:
  - Card (thumb + name)
  - Set / Number
  - Type (RAW/GRADED/SEALED)
  - Condition/Grade
  - Stage (Badge)
  - Listing price
  - Market price
  - Actions (Open)
- Add simple filters at top:
  - search by card name (client-side)
  - stage filter dropdown (optional but preferred)
- Allow changing stage from list view:
  - Stage dropdown per row OR in item detail drawer.
  If implemented per row:
    - optimistic update + mockApi.updateInventoryItem
    - rollback on failure with toast

Files to create/update:
- /app/(app)/inventory/page.tsx  (add toggle + conditional rendering)
- /components/inventory/InventoryList.tsx (new)
- /components/inventory/InventoryHeader.tsx (optional, new)
- /lib/storage.ts (for view mode persistence helper, optional)

------------------------------------------------------------
C) QA / ACCEPTANCE CRITERIA (NON-NEGOTIABLE)
------------------------------------------------------------
- Dragging a card from Stage A to Stage B updates UI immediately and persists after refresh.
- On random mock API failure, stage reverts and toast explains it.
- View toggle works; chosen view persists after refresh.
- List view is fully usable on mobile (responsive table or stacked rows).
- No console errors.
- No broken routes.
- Existing seeded data remains valid.

------------------------------------------------------------
D) OUTPUT REQUIREMENTS
------------------------------------------------------------
Return:
- Updated file list + key diffs
- Any new dependencies added
- The full updated code for new/modified files
- Brief note explaining where DnD logic lives and how to extend it

Do not:
- Add new backend
- Replace the mock API architecture
- Remove existing Kanban functionality



<-- 2. FEATURE PROMPT -->


You are a senior full-stack engineer and domain expert in TCG marketplaces
(Magic, Pokémon, One Piece).

You are working on SLabHub CRM — a professional inventory management system
for TCG sellers and collectors.

Your task is to refactor and extend the Inventory module to strictly follow
real-world TCG domain logic.
Do NOT simplify the model.
Do NOT collapse different asset types into a single generic item.
Respect market-grade distinctions and corner cases.

========================================
CORE DOMAIN PRINCIPLES (MANDATORY)
========================================

1. InventoryItem is an ASSET, not a Card.
2. Card, CardVariant, PhysicalCopy and InventoryItem are different concepts.
3. Condition belongs to InventoryItem, not to Card.
4. Graded cards are NON-FUNGIBLE assets.
5. Raw cards and sealed products ARE fungible and can be aggregated.
6. Alternate Art is a CardVariant, NOT a separate Card.
7. Certification (PSA/BGS/CGC) creates a UNIQUE physical asset via cert_number.

========================================
INVENTORY TYPES
========================================

Inventory must support EXACTLY these asset categories:

1) Single Card — RAW
2) Single Card — GRADED
3) Sealed Product

Each type must:
- have its own required fields
- have its own invariants
- have its own UX flow

========================================
SINGLE CARD — RAW
========================================

Fields:
- card_variant_id (lookup: game, set, number, variant, language)
- condition (NM/LP/MP/HP/DMG)
- quantity (>= 1)
- acquisition_price_per_unit
- acquisition_date
- acquisition_source
- storage_location
- inventory_stage

Rules:
- quantity > 1 allowed
- cert_number FORBIDDEN
- condition REQUIRED
- multiple conditions must be split into separate inventory items

========================================
SINGLE CARD — GRADED
========================================

Fields:
- card_variant_id
- grading_company (PSA / BGS / CGC)
- grade
- cert_number (globally unique)
- slab_images (front/back)
- grading_cost
- acquisition_price
- inventory_stage

Rules:
- quantity MUST be 1
- cert_number REQUIRED
- condition NOT allowed
- each graded card is a unique non-fungible asset
- listing must be 1-to-1 with physical card

Support corner cases:
- crack & resubmit (archive old cert)
- cross-grading
- reholder
- lost slab

========================================
SEALED PRODUCT (LONG-TAIL SAFE)
========================================

Do NOT hardcode product types.

Use a flexible ProductType registry:
- Booster Box
- Booster Pack
- Starter Deck
- Illustration Box
- Mini Tin
- Premium Box
- Gift Box
- Anniversary Set
- Promo Pack
- Tournament Kit
- Case
- Bundle
- Other (admin-reviewed)

Fields:
- product_name
- product_type (registry-based)
- language
- set (nullable)
- edition / print_wave (optional)
- configuration:
    - contains_boosters (bool)
    - booster_sets[]
    - packs_per_unit
    - contains_fixed_cards (bool)
    - contains_promo (bool)
    - bundle_components[] (optional)
- sealed_integrity (mint / minor_dents / damaged / opened)
- quantity
- acquisition_price_per_unit
- acquisition_date
- storage_location
- inventory_stage

Rules:
- sealed products have NO condition
- sealed products can be aggregated
- sealed → opened must spawn raw inventory items

========================================
UX REQUIREMENTS
========================================

- Inventory type selection MUST be the first step
- Form fields MUST change dynamically by inventory type
- Provide presets for:
    - Illustration Box
    - Mini Tin
- Prevent invalid states at UI level
- Use clear seller-grade copy, not developer jargon

========================================
DATA MODEL & VALIDATION
========================================

- Enforce invariants at BOTH UI and backend level
- Use strict schema validation (e.g. Zod / Prisma)
- Prevent:
    - graded items with quantity > 1
    - raw items with cert_number
    - sealed items with condition

========================================
DELIVERABLES
========================================

Produce:
1) Updated Inventory data model
2) Form field specification per inventory type
3) Validation rules
4) Inventory workflow states (raw → grading → graded → listed)
5) Notes explaining how corner cases are handled

Do NOT output explanations of this prompt.
Only output the implementation-ready result.


========================================
CARD VARIANT & LANGUAGE (MANDATORY)
========================================

All single-card inventory items (RAW and GRADED) MUST be linked to a
CardVariant, not directly to a Card.

A CardVariant is uniquely defined by:
- base Card (set_code + card_number)
- variant_type:
    - Normal
    - Alternate Art
    - Parallel / Foil
- language:
    - JP
    - EN

Rules:
- Variant selection is REQUIRED when adding inventory
- Variant determines:
    - market price
    - liquidity
    - legality in some regions
- Alternate Art is NOT a separate Card
- Parallel / Foil is NOT a separate Card
- Language differences MUST create separate CardVariants

Inventory constraints:
- Raw inventory items reference:
    - card_variant_id
    - condition
- Graded inventory items reference:
    - card_variant_id
    - grading metadata (cert_number, grade, grader)

UI / UX requirements:
- Card lookup MUST include:
    - Set
    - Card number
    - Card name
    - Variant type selector
    - Language selector
- Variant preview (image) SHOULD update when variant or language changes

Data model requirements:
- card_variant_id MUST be immutable once inventory is created
- card_variant_id MUST be indexed
- price history and listings MUST resolve through card_variant_id

Validation:
- Prevent creating inventory without specifying variant_type
- Prevent merging inventory across different variants or languages


<-- 3. FEATURE PROMPT -->

You are extending an existing monorepo with:
- NestJS + TypeScript backend (apps/api)
- PostgreSQL
- Prisma (prisma/schema.prisma)
- Workspace scripts already exist

TASK:
Implement a backend service that fetches reference data (“dictionaries”) from justtcg.com API
(using the documentation provided in the screenshot) and stores them into PostgreSQL via Prisma.

Hard requirements:
- API authentication must be via env var JUSTTCG_API_KEY and sent as header: x-api-key
- Base URL must be configurable: JUSTTCG_BASE_URL
- Data sync must be runnable from console as CLI commands (not HTTP endpoints)
- Sync must be idempotent (upsert)
- Must support pagination / cursors / filters if docs require
- Must include logging, progress, and error handling
- Must not break existing API modules

Out of scope:
- payments, OAuth/KYC, subscriptions
- real pricing calls usage (we only build dictionaries sync)
- background jobs scheduler (cron) is optional; main is CLI

------------------------------------------------------------
1) WHAT ARE “DICTIONARIES” (SCOPE)
------------------------------------------------------------
From justtcg API docs (see screenshot), implement syncing for these reference datasets
(use exact endpoints/fields based on the documentation):

At minimum implement:
- games
- sets
- rarities
- conditions
- languages
- printings/variants (if present in docs)
- products/cards catalog basics required to build internal card profiles (if present in docs)
- any other reference endpoints clearly present in the docs sidebar

If the API exposes a “catalog” (cards/products), treat it separately from small dictionaries:
- store minimal fields + foreign keys to dictionaries
- do not attempt to store images/big blobs unless provided as URLs

Key rule:
Sync stores canonical external IDs from justtcg (e.g., id fields) so future updates match.

------------------------------------------------------------
2) MONOREPO & MODULE ORGANIZATION
------------------------------------------------------------
Create a new module in apps/api:

apps/api/src/modules/justtcg
- justtcg.module.ts
- justtcg.client.ts          # HTTP client, auth header, retries, pagination helpers
- justtcg.types.ts           # response types (minimal)
- justtcg.mapper.ts          # mapping API DTO -> DB models
- sync/
   - dictionaries.sync.ts    # orchestrator for all dictionaries
   - catalog.sync.ts         # optional if catalog exists
- cli/
   - justtcg.commands.ts     # CLI entrypoints

Also create a domain module:
apps/api/src/modules/reference-data
- reference-data.module.ts
- services for each dictionary upsert
(or keep it within justtcg module, but keep clean separation)

------------------------------------------------------------
3) CLI COMMANDS (MANDATORY)
------------------------------------------------------------
Commands must be runnable from console, e.g.:

pnpm --filter api justtcg:sync:dictionaries
pnpm --filter api justtcg:sync:all
pnpm --filter api justtcg:sync:catalog

Implementation options:
- Preferred: nest-commander (or a minimal custom bootstrap CLI in Nest)
- Acceptable: ts-node script that imports Nest app context

CLI must:
- Validate env vars (JUSTTCG_API_KEY required)
- Print progress:
  - which dictionary is syncing
  - count received
  - count upserted
  - time taken
- Exit with non-zero code on fatal errors

------------------------------------------------------------
4) HTTP CLIENT REQUIREMENTS (MANDATORY)
------------------------------------------------------------
Implement JustTcgClient:
- sends header x-api-key: process.env.JUSTTCG_API_KEY
- supports timeouts (e.g., 10s)
- retries on 429/5xx with exponential backoff (max 3 retries)
- handles pagination from docs:
  - page/limit OR cursor/nextToken
  - implement a generic “fetchAllPages” helper for endpoints

No external network libs required beyond Nest HttpModule/axios, but you may use:
- @nestjs/axios
- axios-retry (optional)

------------------------------------------------------------
5) DATABASE (PRISMA) REQUIREMENTS
------------------------------------------------------------
Add Prisma models for dictionaries and (if present) catalog.

Guidelines:
- Store external IDs in fields like externalId (unique)
- Use upsert by externalId
- Add indexes for lookups

Example pattern for a dictionary model:
model RefGame {
  id          String   @id @default(cuid())
  externalId  String   @unique
  name        String
  slug        String?  @db.VarChar(128)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

Use similar for sets/rarities/conditions/languages/etc.

If catalog endpoints exist:
- Create model RefCard or RefProduct with:
  - externalId unique
  - name, number, setExternalId or setId FK, rarityId FK, gameId FK
  - imageUrl (string) if provided
- Keep it minimal for Stage 1, but consistent.

Migration must be generated and included.

------------------------------------------------------------
6) SYNC LOGIC (IDEMPOTENT, SAFE)
------------------------------------------------------------
For each dictionary:
- Fetch all items from API (with pagination)
- Normalize/validate fields
- Upsert into DB in batches (e.g., 200–1000 per batch)
- Use Prisma transaction per batch when reasonable

Failure handling:
- If one dictionary fails, overall command should fail,
  but print which dictionary failed and why.
- Provide a “--continueOnError” flag OPTIONAL.

------------------------------------------------------------
7) ENV & CONFIG (MANDATORY)
------------------------------------------------------------
Add env vars:
- JUSTTCG_BASE_URL="https://justtcg.com" (or whatever docs show)
- JUSTTCG_API_KEY="..."

Implement ConfigModule usage:
- Validate on boot (zod or class-validator)
- Do not log API key

------------------------------------------------------------
8) DEV EXPERIENCE
------------------------------------------------------------
Provide:
- README section: how to run dictionary sync locally
- Example commands
- Example .env template

Add root/package scripts (or apps/api scripts):
- justtcg:sync:dictionaries
- justtcg:sync:catalog
- justtcg:sync:all

------------------------------------------------------------
9) TESTING (LIGHTWEIGHT)
------------------------------------------------------------
At minimum:
- Unit tests for mapping functions (optional but preferred)
- Integration test is optional; not required

------------------------------------------------------------
10) OUTPUT
------------------------------------------------------------
Return:
- File tree of new/modified files
- Full code for new/modified files
- Prisma schema changes + migration notes
- Updated package.json scripts
- README instructions

IMPORTANT:
Use the endpoints, query params, and schemas exactly as stated in the justtcg API docs screenshot.
Where the docs show required params, implement them.
Where the docs list response fields, map them into DB appropriately.



<-- 4. FEATURE PROMPT -->


You are extending an existing monorepo with:
- NestJS + TypeScript backend (apps/api)
- PostgreSQL
- Prisma
- Workspace scripts

TASK:
Implement a service that syncs reference data (“dictionaries”) from JustTCG API into PostgreSQL.
Sync must be runnable from console (CLI commands), using env var JUSTTCG_API_KEY sent as header "x-api-key".

IMPORTANT CONSTRAINT:
Do NOT assume you can “read documentation from screenshots”.
Therefore: implement dictionaries based on an explicit in-code mapping table/config that lists endpoints and fields.
The endpoints/fields must be easy to adjust by editing a single config file.

------------------------------------------------------------
0) ENV / CONFIG (MANDATORY)
------------------------------------------------------------
Env vars:
- JUSTTCG_BASE_URL (default sensible value)
- JUSTTCG_API_KEY (required)

Auth:
- Send header: x-api-key: JUSTTCG_API_KEY

Add config validation at boot (zod/class-validator). Do not log the api key.

------------------------------------------------------------
1) IN-CODE MAPPING TABLE (MANDATORY, SINGLE SOURCE OF TRUTH)
------------------------------------------------------------
Create a single mapping table (config) that drives:
- which endpoints to call
- how to map response items to Prisma models
- which unique key to use for upsert
- which fields to store

Place it in:
apps/api/src/modules/justtcg/justtcg.mappings.ts

Mapping structure (example, adjust as needed):
type JustTcgMapping = {
  name: string;                 // "games"
  endpoint: string;             // "/v1/games"
  pagination: "none" | "page" | "cursor";
  unique: { sourceField: string; targetField: string }; // e.g. "id" -> "externalId"
  fields: Array<{ source: string; target: string; transform?: "string"|"number"|"json"|"date" }>;
  model: "RefGame" | "RefSet" | ...;  // Prisma model name
};

The sync runner must iterate this mapping table and perform:
- fetch all pages (if needed)
- normalize fields
- upsert into DB using the unique mapping

------------------------------------------------------------
2) TABLE: endpoint → model → unique key → fields (REQUIRED IN THIS PROMPT)
------------------------------------------------------------
Use the table below as the initial mapping defaults.
These are “reasonable defaults” for typical JustTCG dictionary APIs.
They MUST be implemented as code config and be editable in one place.
If the API differs, adjusting this config must be enough (no rewriting business logic).

TABLE (initial defaults):

1) Games
- Endpoint: /v1/games
- Prisma model: RefGame
- Unique key: response.id -> RefGame.externalId
- Fields:
  - id -> externalId
  - name -> name
  - slug -> slug (optional)
  - updatedAt or updated_at -> sourceUpdatedAt (optional)

2) Sets
- Endpoint: /v1/sets
- Prisma model: RefSet
- Unique key: response.id -> RefSet.externalId
- Fields:
  - id -> externalId
  - name -> name
  - code -> code (optional)
  - gameId -> gameExternalId (optional; store as string) OR map to gameId FK if available
  - releasedAt/releaseDate -> releaseDate (optional)
  - slug -> slug (optional)

3) Rarities
- Endpoint: /v1/rarities
- Prisma model: RefRarity
- Unique key: response.id -> RefRarity.externalId
- Fields:
  - id -> externalId
  - name -> name
  - code -> code (optional)

4) Conditions
- Endpoint: /v1/conditions
- Prisma model: RefCondition
- Unique key: response.id -> RefCondition.externalId
- Fields:
  - id -> externalId
  - name -> name
  - abbreviation -> abbreviation (optional)

5) Languages
- Endpoint: /v1/languages
- Prisma model: RefLanguage
- Unique key: response.id -> RefLanguage.externalId
- Fields:
  - id -> externalId
  - name -> name
  - code -> code (optional)

6) Printings / Variants (if exists)
- Endpoint: /v1/printings (or /v1/variants)
- Prisma model: RefPrinting
- Unique key: response.id -> RefPrinting.externalId
- Fields:
  - id -> externalId
  - name -> name
  - code -> code (optional)

7) Catalog: Products/Cards (optional but supported via mapping)
- Endpoint: /v1/products (or /v1/cards)
- Prisma model: RefProduct (or RefCard)
- Unique key: response.id -> RefProduct.externalId
- Fields (minimal):
  - id -> externalId
  - name -> name
  - number -> number (optional)
  - imageUrl/image_url -> imageUrl (optional)
  - setId -> setExternalId (optional)
  - rarityId -> rarityExternalId (optional)
  - gameId -> gameExternalId (optional)

IMPORTANT:
- “Optional but supported”: Catalog sync may be behind a separate CLI command.
- For relations you can start by storing external IDs as plain strings,
  and only later convert to FK relations. Prefer MVP speed + correctness.

------------------------------------------------------------
3) PRISMA MODELS (MANDATORY)
------------------------------------------------------------
Add Prisma models for dictionaries. Use externalId UNIQUE.
Example pattern:

model RefGame {
  id          String   @id @default(cuid())
  externalId  String   @unique
  name        String
  slug        String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

Create similar:
- RefSet (externalId, name, code?, slug?, gameExternalId?, releaseDate?)
- RefRarity (externalId, name, code?)
- RefCondition (externalId, name, abbreviation?)
- RefLanguage (externalId, name, code?)
- RefPrinting (externalId, name, code?) // optional
- RefProduct/RefCard (externalId, name, number?, imageUrl?, setExternalId?, rarityExternalId?, gameExternalId?) // optional

Generate migration(s).

------------------------------------------------------------
4) MODULE ORGANIZATION
------------------------------------------------------------
apps/api/src/modules/justtcg
- justtcg.module.ts
- justtcg.client.ts           # http client (auth header, retries, pagination)
- justtcg.mappings.ts         # THE mapping table config
- sync/justtcg.sync.service.ts# orchestrator: reads mapping, fetches, upserts
- cli/justtcg.commands.ts     # CLI entrypoints
- justtcg.types.ts            # response types minimal

Use ConfigModule for env.

------------------------------------------------------------
5) HTTP CLIENT REQUIREMENTS
------------------------------------------------------------
- Use @nestjs/axios (HttpModule) or fetch/ofetch equivalent
- Timeout 10s
- Retry on 429 and 5xx up to 3 times with exponential backoff
- Pagination support:
  - page-based (page, limit)
  - cursor-based (cursor/nextCursor/next_token)
Implement a generic fetchAll() that takes mapping.pagination and returns all items.

------------------------------------------------------------
6) UPSERT LOGIC (MANDATORY)
------------------------------------------------------------
- For each mapping:
  - fetch all items
  - map each item using fields transforms
  - upsert in batches (e.g., 500)
  - unique where: { externalId: mappedExternalId }

- Log:
  - dictionary name
  - total fetched
  - total upserted
  - duration

- On error:
  - fail command with non-zero exit code
  - print which mapping failed

------------------------------------------------------------
7) CLI COMMANDS (MANDATORY)
------------------------------------------------------------
Implement console commands (use nest-commander preferred):

- justtcg:sync:dictionaries
  - runs all mappings EXCEPT catalog (products/cards)
- justtcg:sync:catalog
  - runs only catalog mapping(s) if present
- justtcg:sync:all
  - runs everything

Also support flags:
- --only=games,sets (optional)
- --dryRun (optional): prints counts and sample mapped record, writes nothing

Add scripts in apps/api/package.json (or root):
- justtcg:sync:dictionaries
- justtcg:sync:catalog
- justtcg:sync:all

------------------------------------------------------------
8) OUTPUT
------------------------------------------------------------
Return:
- file tree of new/modified files
- full code for new/modified files
- prisma schema changes
- scripts
- README instructions:
  - env vars
  - commands to run
  - how to adjust mapping config (endpoints/fields) without rewriting logic

DO NOT:
- implement real external pricing usage
- implement auth beyond env header
- require screenshot parsing



<-- 5. FEATURE PROMPT -->

You are extending an existing product with:
- Frontend: Next.js (App Router) + TS + Tailwind + shadcn/ui
- Backend: NestJS + TS + PostgreSQL + Prisma (monorepo)
- Inventory “Add Item” flow already exists and supports itemType=GRADED with detailed fields.
Goal:
Simplify the graded slab ingestion UX so users only enter:
1) Grader (PSA or BGS)
2) Certification number
Then the app automatically fetches and parses the grader certificate page to populate all available fields.

------------------------------------------------------------
A) UX CHANGES (FRONTEND)
------------------------------------------------------------
Update the “Add Item → Graded” step UI:

1) Replace existing graded slab inputs with minimal form:
- Grader selector: PSA | BGS (required)
- Certification # input (required, numeric)
- Button: “Fetch details” (primary)
- Auto-fetch on blur or after 500ms debounce is optional; explicit button is acceptable.

2) After fetch success, render a “Parsed details” preview card:
- Card title/name
- Set/series
- Card number (if found)
- Grade (PSA 10, BGS 9.5 etc.)
- Any extra fields available (year, parallel/variant, attributes)
- Images (if present as URLs; otherwise skip)

3) Provide an “Edit manually” accordion (collapsed by default):
- Allows user override of parsed fields (name, set, grade, etc.)
- But keep minimal by default.

4) Show loading skeleton and clear error states:
- “Cert not found”
- “Parsing failed”
- “Rate limited / blocked”
- Retry button

5) Quantity must be locked to 1 for graded slabs (keep current behavior).

6) Ensure the final saved InventoryItem contains:
- gradeProvider = PSA or BGS
- certificationNumber stored
- gradeValue / gradeLabel stored
- parsed metadata stored as structured JSON in item “gradingMeta” (new field) or photos/meta json.

Add a new optional field on InventoryItem:
- certificationNumber (string)
- gradingMeta (json) // parsed data raw for auditing/debug

------------------------------------------------------------
B) BACKEND SERVICE (NESTJS) — CERT PARSING
------------------------------------------------------------
Implement a backend module that:
- Accepts grader + cert number
- Fetches the public certificate page HTML
- Parses required fields
- Returns structured JSON to frontend

Create:
apps/api/src/modules/grading
- grading.module.ts
- grading.controller.ts
- grading.service.ts
- parsers/
   - psa.parser.ts
   - bgs.parser.ts
- http/grading-http.client.ts (handles fetch + headers + retries)
- dto/lookup.dto.ts
- types/grading.types.ts

Endpoint:
POST /v1/grading/lookup
Body:
{
  "grader": "PSA" | "BGS",
  "certNumber": "112983707"
}

Response (example):
{
  "grader": "PSA",
  "certNumber": "112983707",
  "success": true,
  "data": {
    "gradeLabel": "PSA 10",
    "gradeValue": 10,
    "cardName": "...",
    "setName": "...",
    "cardNumber": "...",
    "year": "...",
    "variant": "...",
    "population": null,
    "images": { "frontUrl": "...", "backUrl": "..." },
    "raw": { ... } // optional raw parsed map
  }
}

Failure:
- 404 when cert not found
- 422 when page fetched but parsing failed
- 429 when upstream rate limited
- 502 when upstream blocked or HTML inaccessible

Important:
- Do NOT scrape from frontend (CORS, exposure); do it server-side only.
- Add caching to reduce repeated hits:
  - cache by (grader, certNumber) for 24h (in-memory is OK for MVP; Redis optional).
- Add rate limiting per IP/user to prevent abuse (simple Nest throttler acceptable).

------------------------------------------------------------
C) PSA CERT PAGE (MANDATORY SUPPORT)
------------------------------------------------------------
Support PSA cert page parsing using URL format:
https://www.psacard.com/cert/{certNumber}/psa

Implementation requirements:
- Fetch HTML with a realistic user-agent header
- Parse using a robust HTML parser (cheerio recommended)
- Extract:
  - grade (numeric or label)
  - card name / subject line
  - set/series
  - card number (if present)
  - any extra attributes shown on page
- Be resilient:
  - selectors may change; implement parsing with fallback strategies:
    - primary selectors
    - regex fallback over text blocks

------------------------------------------------------------
D) BGS SUPPORT (OPTIONAL BUT PREFERRED)
------------------------------------------------------------
If BGS certificate lookup has a known URL pattern, implement it.
If unknown, implement BGS as a stub that returns “NOT_IMPLEMENTED” but keep the architecture ready.

For BGS stub:
- return 501 with message “BGS parsing not yet configured”
- frontend shows friendly message and allows manual entry fallback.

(If you know the BGS cert URL format from your codebase/config, use it.)

------------------------------------------------------------
E) DATABASE CHANGES (PRISMA)
------------------------------------------------------------
Update Prisma InventoryItem model:
- add certificationNumber String? (indexed)
- add gradingMeta Json? (optional)
- ensure gradeProvider + gradeValue already exist or add them.

Migration required.

When saving a graded item:
- certificationNumber must be stored
- gradeProvider set from selected grader
- gradeValue / gradeLabel stored
- gradingMeta stores parsed object

------------------------------------------------------------
F) INVENTORY CREATION FLOW (FRONTEND + BACKEND)
------------------------------------------------------------
Frontend flow:
1) user selects grader + cert#
2) calls POST /v1/grading/lookup
3) displays parsed preview
4) user clicks “Continue / Save”
5) create inventory item payload includes:
   - itemType="GRADED"
   - gradeProvider
   - certificationNumber
   - gradeValue
   - gradingMeta (parsed data)
   - cardProfileId resolution:
       - Attempt to match parsed info to existing CardProfile by:
         - card number + set name
         - fallback: name contains + set contains
       - If no confident match:
         - allow user to manually search/select CardProfile (required)
       - Do NOT auto-create new CardProfile in Stage 1 unless existing system already supports it.

Important:
- If parsed data indicates a known card already in catalog, preselect it and show confidence badge.

------------------------------------------------------------
G) NON-FUNCTIONAL REQUIREMENTS
------------------------------------------------------------
- Do not violate robots.txt intentionally, but implement minimal MVP.
- Add timeouts + retries.
- Do not store full HTML; store only parsed fields.
- Add safe logging (no PII).
- Ensure code is modular: adding CGC later should be easy.

------------------------------------------------------------
H) ACCEPTANCE CRITERIA
------------------------------------------------------------
1) Graded slab form is simplified to grader + cert#.
2) PSA example works:
   - grader=PSA, certNumber=112983707
   - backend fetches https://www.psacard.com/cert/112983707/psa
   - returns parsed grade + card metadata
3) User can save the graded item with quantity locked to 1.
4) If parsing fails, user can still proceed via “manual entry fallback”.
5) Result persists in DB with certificationNumber + gradingMeta.

------------------------------------------------------------
I) OUTPUT
------------------------------------------------------------
Return:
- updated frontend files (new UI + API call)
- new backend module files
- prisma schema + migration
- README notes: how parsing works, how to add BGS URL/selectors, known limitations


<-- 6. FEATURE PROMPT -->


You are extending an existing product with:
- Frontend: Next.js (App Router) + TypeScript + Tailwind + shadcn/ui
- Backend: NestJS + TypeScript + PostgreSQL + Prisma
- Monorepo setup
- Existing Market Pricing UI page already implemented visually

GOAL:
Bind the Market Pricing page to real backend data using RefProduct as the source of truth,
and extend the page with pagination, unified search, and a detailed price history drawer.

------------------------------------------------------------
A) DATA BINDING (CORE REQUIREMENT)
------------------------------------------------------------
The Market Pricing page must be driven by backend data.

Source of truth:
- Backend entity: RefProduct (from reference data / JustTCG sync)

Field mapping (STRICT):
- UI "Card Name" → RefProduct.name
  Example: "Monkey.D.Luffy"
- UI "Number" (subtitle) → RefProduct.number
  Example: "OP01-001"
- UI "Set" → mock/static value for now (can be placeholder)
- Raw Price → mock value (generated on backend)
- Sealed Price → mock value (nullable)
- Last Updated → mock timestamp
- Source → mock enum: "Mock:eBay" | "Mock:TCGPlayer"

IMPORTANT:
- Do NOT introduce a new Product/Card model.
- Do NOT compute pricing on frontend.
- Frontend must consume backend API only.

------------------------------------------------------------
B) BACKEND API (MANDATORY)
------------------------------------------------------------
Implement or extend backend endpoints:

1) Product list for Market Pricing
GET /v1/market/products

Query params:
- page (number, default 1)
- limit (number, default 25, max 100)
- search (string, optional)

Behavior:
- Returns paginated list of RefProduct
- Search must match:
  - name ILIKE %search%
  - OR number ILIKE %search%
- Order by name ASC by default

Response shape:
{
  "items": [
    {
      "id": "...",
      "name": "Monkey.D.Luffy",
      "number": "OP01-001",
      "rawPrice": 42.53,
      "sealedPrice": null,
      "lastUpdated": "2026-01-26T19:53:59Z",
      "source": "Mock:eBay"
    }
  ],
  "page": 1,
  "limit": 25,
  "total": 312
}

Notes:
- rawPrice / sealedPrice / lastUpdated / source may be mocked/generated server-side.
- Keep logic centralized in backend.

------------------------------------------------------------
2) Product price history (detail drawer)
------------------------------------------------------------
GET /v1/market/products/:id/prices

Behavior:
- Returns last 10 price points for the product
- Data is MOCKED but realistic

Response shape:
{
  "productId": "...",
  "prices": [
    {
      "date": "2026-01-20",
      "title": "Monkey D. Luffy OP01-001 PSA 10",
      "price": 44.12,
      "source": "eBay"
    },
    {
      "date": "2026-01-18",
      "title": "OP01-001 Monkey D. Luffy Romance Dawn",
      "price": 41.99,
      "source": "TCGPlayer"
    }
  ]
}

Rules:
- Always return max 10 entries
- Mix eBay / TCGPlayer in mock data
- Prices should fluctuate realistically (+/- 10–20%)

------------------------------------------------------------
C) FRONTEND: MARKET PRICING PAGE
------------------------------------------------------------
Bind the existing Market Pricing page to backend APIs.

Features to implement:

1) Pagination (MANDATORY)
- Server-side pagination
- Show current page, total pages
- Controls:
  - Prev / Next
  - Page numbers OR compact pager
- Changing page must re-fetch data

2) Unified Search (MANDATORY)
- Single input: “Search card profiles”
- Search must:
  - debounce (300–500ms)
  - send `search` query param
  - match by name OR number
- Reset page to 1 on new search

3) Detail Drawer (MANDATORY)
- Clicking a row opens a Drawer/Sheet (shadcn/ui)
- Drawer content:
  - Card name + number
  - Placeholder image
  - Current raw/sealed price
  - Source badge
  - “Last updated”
  - Divider
  - “Recent Sales” section:
      - Table or list of last 10 prices
      - Columns:
          - Date
          - Title
          - Price
          - Source (badge: eBay / TCGPlayer)
- Drawer fetches data from:
  GET /v1/market/products/:id/prices

UX requirements:
- Show loading skeleton while fetching
- Handle error state gracefully
- Drawer must be scrollable
- Mobile-friendly layout

------------------------------------------------------------
D) FRONTEND STATE & QUALITY
------------------------------------------------------------
- Keep pagination/search state in URL query params (?page=&search=)
- Avoid unnecessary re-fetches
- Show empty state if no results
- No mock pricing logic in frontend
- No hardcoded product lists

------------------------------------------------------------
E) BACKEND IMPLEMENTATION NOTES
------------------------------------------------------------
- Implement pagination with Prisma:
  - skip / take
  - total count query
- Implement search using ILIKE
- Mock pricing logic should live in a service:
  MarketPricingService
- Price history can be generated dynamically per request (no DB persistence needed for now)

------------------------------------------------------------
F) ACCEPTANCE CRITERIA
------------------------------------------------------------
1) Market Pricing page loads products from backend RefProduct table.
2) Search by “Luffy” or “OP01-001” works from single input.
3) Pagination works and reflects total count.
4) Clicking a row opens a detail drawer.
5) Drawer shows last 10 mocked prices with date, title, price, and source.
6) No pricing logic exists in frontend.
7) Page remains responsive and usable on mobile.

------------------------------------------------------------
G) OUTPUT
------------------------------------------------------------
Return:
- Updated backend controllers/services
- Updated frontend page + drawer components
- Any new DTOs or types
- Brief note explaining mock pricing strategy and how it will be replaced later